import { StyleSheet } from 'react-native';

export const playerStyles = StyleSheet.create({
    player: {
        position: 'absolute',
    },
    astronaut: {
        // Стили для анимации космонавта
    },
    default: {
        // Стили для персонажа по умолчанию
    },
});