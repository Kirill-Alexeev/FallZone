import { StyleSheet } from 'react-native';

export const statsDisplayStyles = StyleSheet.create({
    container: {
        alignItems: 'center',
        marginTop: 20,
    },
    text: {
        color: '#FFF',
        fontSize: 16,
    },
});