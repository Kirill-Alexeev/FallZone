import { StyleSheet } from 'react-native';

export const characterPreviewStyles = StyleSheet.create({
    container: {
        alignItems: 'center',
        justifyContent: 'center',
        marginVertical: 30,
        height: 200, // Фиксированная высота для контейнера
    },
    animation: {
        // Дополнительные стили если нужны
    },
});